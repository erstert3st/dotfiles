# repo docs and tests are not dotfiles - never deploy them to ~.
README.md
CLAUDE.md
tests
tests/**

# claude code writes runtime state next to its settings; never manage these.
# .credentials.json holds an oauth token - it must not end up in the repo.
.claude/.credentials.json
.claude/history.jsonl
.claude/stats-cache.json
.claude/*.bak
.claude/*.orig
.claude/backups
.claude/projects
.claude/sessions
.claude/session-env
.claude/shell-snapshots
.claude/tasks
.claude/teams
.claude/telemetry
.claude/todos
.claude/ide
.claude/plugins
.claude/statsig
.claude/hooks

{{ if not (and (hasKey . "desktop") .desktop) }}
# GUI configs only land on desktop machines (answer 'desktop' at chezmoi init)
.config/hypr
.config/hypr/**
.config/ghostty
.config/ghostty/**
{{ end }}
